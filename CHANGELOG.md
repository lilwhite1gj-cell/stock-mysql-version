# 更新日志 (Changelog)

所有重要变更均会记录在此文件中。

> ## 📌 维护约定（必读）
> - **版本号 `1.2.N` 由 CI 自动生成**：每次推送 `main`，GitHub Actions 会根据总提交数自动改写 `package.json` 的 `version`、前端 `appVersion` 变量与版本徽章为 `1.2.N`（N 为提交总数），无需手动修改。
> - **更新日志与版本号自动同步**：CI 会自动把更新日志顶部第一个版本标题（`## [x.y.z]`）改写为生成的 `## [1.2.N] - 当天日期`；但"改了什么"仍需手动填写，CI 不会自动总结。
> - **开发流程**：每次新增/修改功能后，请在本文件顶部手动添加一节 `## [1.2.N] - YYYY-MM-DD` 并填写变更要点（参考下方历史格式），再提交推送。版本号以部署后 `/api/version` 实际显示的 `1.2.N` 为准。

## [1.2.85] - 2026-07-30

### 新增
- 🔀 收发明细视图范围切换：收发明细页新增"视图范围"分段切换控件，支持两种模式——「仅自己」（默认，仅展示当前登录用户的业务流水）与「全部」（展示系统内全部用户的业务流水）。切换即时生效，无需刷新页面；默认选中"仅自己"，既有字段展示与排序方式保持不变，仅按所选模式过滤数据范围。后端 `GET /api/transactions` 新增 `scope` 参数（`self`/`all`），MySQL 与本地 JSON 双模式均已支持

### 修复
- 🐛 库存台账「商品生命周期」不显示流转记录：根因为生产 MySQL 模式下 `GET /api/transactions` 返回的交易记录仅含 `product_id`（蛇形列名），前端详情页按 `t.productId`（驼峰）过滤导致永远匹配不到，生命周期恒为空。已在后端响应中统一补上 `productId` 映射（MySQL: `product_id` → `productId`；JSON: 兜底 `productId ?? product_id`），前端过滤逻辑增加 `?? t.product_id` 兜底。修复后初次入库/出库均能在商品档案全景看板的「全生命周期流转追溯」中正确展示；同时该修复一并修复了收发明细列表与流水详情页（`showTransDetail`）按 `t.productId` 关联产品名的功能（此前在 MySQL 模式下会显示"已删"/"产品已被移除"）
- 🐛 库存台账「资产价值」列与工厂名在 MySQL 模式下显示异常：前端从 `GET /api/inventory` 读取 `unitPrice`/`factoryId`/`createdBy`/`creatorName`（驼峰），但 MySQL 返回的是 `unit_price`/`factory_id`/`created_by`/`creator_name`（蛇形），导致库存台账资产价值列显示 ¥0、工厂名空白、仪表盘估值归零。已在 `GET /api/inventory` 响应中补充驼峰别名（`unitPrice: unit_price ?? unitPrice` 等），JSON 模式下 `??` 回退到原值无副作用。修复后资产价值、工厂关联、按金额排序均恢复正常
- 🐛 收发明细录入后产品名显示"已删"：`renderTrans` 直接依赖全局 `products` 数组，但用户从非台账/仪表盘页直接进入收发明细或刷新页面时 `products` 尚未加载，导致 `products.find` 取不到产品而 fallback 为"已删"。已在 `renderTrans` 开头增加兜底逻辑：若 `products` 为空则自动请求 `/inventory` 加载产品数据后再渲染；同时后端 `GET /api/transactions` 的 `productId` 映射增加空值保护，避免 `product_id` 为空时生成 `"null"`/`"undefined"` 字符串。修复后无论从何页面进入，收发明细列表与录入后均正确显示产品名称

## [1.2.0] - 2026-07-29

### 新增
- 📋 操作审计日志功能：新增 `audit_logs` 数据表，自动记录所有写操作（产品/流水/用户/工厂/客户/品类的创建/修改/删除、角色变更、密码重置、批量导入、备份导入），管理后台新增"操作日志"页面，支持按用户/操作类型/时间范围筛选与分页查询
- 📊 Excel 报表导出功能：使用 `exceljs` 库实现三种 .xlsx 格式导出——库存汇总表（含合计行）、收发明细表（支持日期/类型筛选）、产品估值表（含总估值汇总），格式美观含表头样式
- 📥 批量导入功能：支持 Excel(.xlsx) 文件解析，实现产品批量导入（自动 SKU 去重、事务回滚）和流水批量导入（按产品名称/SKU 匹配、字段校验），提供模板下载，导入后返回成功/跳过/错误明细
- 📥 批量导入工具卡片：系统设置页新增"批量导入工具"区域，提供产品模板和流水模板的一键下载入口

### 优化
- 📱 移动端适配进一步优化：新增 `touch-action: manipulation` 消除 300ms 点击延迟；`overscroll-behavior: contain` 防止弹窗/表格滚动穿透；按钮最小触控高度 36px、输入框最小高度 48px 提升触控体验；禁用列表项长按选中防止误触；操作日志页面移动端单列筛选布局；小屏(≤375px)按钮尺寸进一步缩减

## [1.1.70] - 2026-07-30

### 修复
- 🐛 修复数据备份导入丢失 `receiver`（领用人）和 `batch_no`（批次号）字段的问题：备份恢复时流水记录的两列未写入数据库，现已补全 INSERT 语句与字段映射，确保全量数据无损恢复
- 🐛 修复编辑产品档案时材质字段始终被清空的问题：`openEditModal` 中属性名映射错误（`p.mat` 应为 `p.material`），导致编辑后材质丢失，已修正映射并增加显式赋值
- 🐛 修复工厂档案随机颜色生成可能产生无效 hex 值的问题：`Math.random()` 结果转十六进制后未补零，小数值会生成 `#ff` 等短色值，已添加 `padStart(6, '0')` 确保始终为 6 位合法 hex
- 🐛 修复 `/api/admin/change-role` 接口缺少角色值白名单校验的问题：可设置任意字符串为角色（如 `superadmin`），破坏权限体系，已增加 `admin/manager/staff` 白名单校验
- 🐛 修复 `/api/admin/change-role` 接口允许管理员降级自身的问题：误操作可导致权限丢失，已禁止自我角色调整
- 🐛 修复 `/api/admin/change-role` 接口允许降级最后一位管理员的问题：降级后系统将无管理员可用，已增加最后管理员保护校验
- 🐛 修复产品详情面板和流水详情面板缺少空值保护的问题：产品或流水被删除后点击详情会抛出异常，已增加 null 检查与友好提示
- 🐛 修复品类删除使用原生 `confirm()` 弹窗的问题：与系统全局 `customConfirm` 风格不一致，且部分环境下原生弹窗异常，已统一替换为 `customConfirm`

### 修复（含上轮未推送）
- 🐛 修复客户名隐私权限"通用/受限"显示不一致的问题：不填客户时 `getOrCreateCustomer` 返回 `{ name: '通用' }`，该字符串存入 DB 后触发 `customerRestricted = true`，导致其他用户看到"🔒 受限"而非空白，已改为返回空字符串并清理 DB 中已有的"通用"记录

## [1.1.67] - 2026-07-30

### 新增
- 🔍 收发流水（入库/出库）产品选择下拉增加关键词模糊搜索：支持产品名称模糊匹配（不区分大小写）；未输入关键词时显示全部产品；输入即时刷新筛选结果；保留原有选中 / 清空 / 默认值等交互，入库出库其他功能不受影响

## [1.1.50] - 2026-07-23

### 优化
- 🎨 前端 UI 全面优化（可读性 / 响应式 / 动效 / 无障碍），仅调整视觉与体验，交互逻辑与功能保持不变
- 🔍 文字可读性：提升正文/字段标签/占位符字号与行高，提亮浅色背景上的低对比度文字（slate-400/500/300）
- 📱 响应式：桌面端内容区最大宽度 1440px 居中避免拉伸；新增平板（768–1023px）专属间距；移动端表单上下堆叠、按钮自适应、无横向滚动
- ✨ 动效：桌面端弹窗/遮罩平滑入场（150–300ms）；按钮点击触感反馈；新增 `.btn-loading` 加载态样式；页面切换与卡片悬停过渡保持流畅
- ♿ 无障碍：新增键盘聚焦可见环；尊重系统「减少动效」偏好自动关闭动画
- 📜 更新日志弹窗内版本号、时间戳与条目列表的字号与对比度提升

## [1.1.32] - 2026-07-23

### 修复
- 🐛 修复注册API返回500错误：saveLocalData()在data目录不存在时崩溃
- 🐛 修复登录返回401问题：添加诊断端点排查MySQL连接状态
- 🛡️ saveLocalData()自动创建data目录（递归创建）
- 🛡️ 注册API本地JSON路径添加try-catch错误处理

### 新增
- 🔍 新增 /api/health 诊断端点，显示数据库模式、MySQL连接状态、用户数量等

## [1.1.0] - 2026-07-22

### 新增
- 🏷️ 版本号与更新日志系统，支持前端页面查看版本信息
- 🔄 GitHub Actions 自动部署工作流（推送到 main 分支自动同步至 Hostinger FTP）
- 🔒 服务端安全加固：CORS 收敛、请求限流、安全响应头
- 🛡️ 前端 XSS 防护：输入转义、上传文件类型校验

### 修复
- 🐛 修复删除流水记录时"撤销失败"无具体错误原因的问题，现在会显示服务端返回的详细错误信息
- 🐛 修复负库存校验删除入库记录时前端只显示通用提示的问题

### 变更
- ⬆️ cloudinary 依赖曾升级至 v2.7.0（修复 CVE-2025-12613 任意参数注入），后因与 multer-storage-cloudinary 依赖冲突降级回 1.x（当前固定 1.41.3）
- ⬆️ express 升级至 v5.2.1
- ⬆️ multer 升级至 v2.2.0

---

## [1.0.0] - 2026-07-10

### 核心功能
- 📦 库存台账管理：产品 CRUD、分类、SKU 货号、规格材质
- 📊 仪表盘看板：库存总览、收发统计、趋势图表
- 🔄 收发明细：入库/出库流水记录、图片凭证、撤销与库存回滚
- 🏭 工厂档案：供应商信息管理
- 👥 客户档案：客户信息与联系方式管理
- 🔐 用户认证：注册/登录、JWT Token、角色权限（admin/staff）
- 👑 账号管理：管理员可增删用户、调整角色权限
- 🏷️ 品类维护：自定义产品分类
- ⚠️ 库存预警：低库存自动提醒（阈值 10）
- 💱 汇率转换：CNY/USD 双币种支持，实时汇率查询
- 📤 数据导出：库存 CSV 导出、流水 CSV 导出
- 💾 数据备份：JSON 全量备份导出/导入恢复
- 🖼️ 图片上传：Cloudinary 云存储集成
- 📱 响应式设计：桌面端侧边栏 + 移动端底部导航
- 🗄️ MySQL 数据库：生产环境 MySQL + 开发环境 JSON 文件双模式